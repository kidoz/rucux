# Images

This directory is reserved for installable and bootable image assembly inputs.

Examples that belong here:

- partition layouts
- filesystem overlays
- VM bundle descriptions
- installer media definitions
- post-build image assembly configuration

Kernel and userspace binaries are build outputs. This directory should describe how those outputs become distributable artifacts.
